# Chau7 pre-commit review configuration
#
# enabled: 1|0
# gate: advisory | high | any | off
# timeout_ms: integer milliseconds
# model: optional provider-specific model override
# backend: review backend to launch through Chau7 runtime

enabled = 1
gate = advisory
timeout_ms = 45000
model =
backend = codex
