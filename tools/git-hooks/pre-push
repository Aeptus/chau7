#!/bin/zsh
# Pre-push hook: runs full CI checks before allowing push.
# Skip with: git push --no-verify

set -e

cd "$(git rev-parse --show-toplevel)/apps/chau7-macos"

echo "🔍 Running pre-push checks..."
just check
